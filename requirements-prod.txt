# Production requirements with flexible versions for better compatibility
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
sqlalchemy>=2.0.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0
psutil>=5.9.0
requests>=2.31.0

# Development and testing (optional in production)
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0

# Security dependencies
cryptography>=42.0.0
bcrypt==4.1.2

# Database (if using PostgreSQL in production)
psycopg2-binary==2.9.9

# Additional production dependencies
gunicorn==21.2.0  # Alternative ASGI server
prometheus-client==0.23.1  # For metrics export (updated version)
sentry-sdk[fastapi]==1.39.1  # Error tracking

# Recently added dependencies
aiosqlite==0.19.0  # Async SQLite
redis==5.0.1  # Redis client
pydantic-settings==2.1.0  # Settings management